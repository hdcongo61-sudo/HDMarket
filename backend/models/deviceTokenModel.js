import PushToken from './pushTokenModel.js';

// Backward-compatible alias to keep legacy PushToken usage intact
// while exposing an explicit DeviceToken model in new notification flows.
export default PushToken;
